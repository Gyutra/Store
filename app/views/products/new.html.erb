<h1>New Product:</h1>

<hr>

<%= form_with scope: :product, url: new_category_product_path, local: true do |f| %>

	  <% if @product.errors.any? %>category.product.new(params[:category_id])
    <div id="error_explanation">
      <h2>
        <%= pluralize(@product.errors.count, "error") %> prohibited
        this product from being saved:
      </h2>
      <ul>
        <% @product.errors.full_messages.each do |msg| %>
          <li><%= msg %></li>
        <% end %>
      </ul>
    </div>
  <% end %>


	<p>
		<%= f.label :name %>
		<%= f.text_field :name %>
	</p>

	<p>
		<%= f.file_field :image %>
	</p>

	<p>
		<%= f.submit %>
	</p>
	<% end %>
